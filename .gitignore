# C-Gate runtime artifacts
cgate-server/cgate-dist/com/
cgate-server/cgate-dist/logs/
C-Gate-Native-Logs/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Local analysis output
graphify-out/

# Go module files are synthesised at build time (see Dockerfile / CI),
# not committed — running go tooling locally recreates them.
cgate-server/web/go.mod
cgate-server/web/go.sum
